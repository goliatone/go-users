// Package registry contains interfaces and default implementations for the
// custom role registry. Default structs compose go-repository-bun repositories
// but can be replaced by the host application via dependency injection.
package registry
