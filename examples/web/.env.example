# Server Configuration
SERVER_PORT=8978
SERVER_HOST=localhost

# Database Configuration
DB_DSN=file::memory:?cache=shared

# Auth Configuration
AUTH_SIGNING_KEY=change-me-in-production-to-a-secure-random-string
AUTH_TOKEN_TTL=24h

# Views Configuration
VIEWS_DIR=./views
VIEWS_ASSETS_DIR=./public
